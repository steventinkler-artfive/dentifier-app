{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user"
      ],
      "default": "user"
    },
    "is_beta_tester": {
      "type": "boolean",
      "default": false,
      "description": "True if the user is a beta tester and bypasses subscription checks"
    },
    "subscription_tier": {
      "type": "string",
      "enum": [
        "starter",
        "professional",
        "founder",
        "early_bird"
      ],
      "default": "starter",
      "description": "The user's subscription tier"
    },
    "subscription_status": {
      "type": "string",
      "enum": [
        "trialing",
        "active",
        "past_due",
        "cancelled"
      ],
      "description": "Current subscription status"
    },
    "subscription_plan": {
      "type": "string",
      "description": "Stripe subscription plan identifier"
    },
    "stripe_customer_id": {
      "type": "string",
      "description": "Stripe customer ID for this user"
    },
    "stripe_subscription_id": {
      "type": "string",
      "description": "Stripe subscription ID for this user"
    },
    "subscription_end_date": {
      "type": "string",
      "format": "date-time",
      "description": "Date the current subscription period ends"
    },
    "trial_end_date": {
      "type": "string",
      "format": "date-time",
      "description": "Date the trial period ends"
    },
    "trial_used": {
      "type": "boolean",
      "default": false,
      "description": "Set to true the first time a trial subscription starts for this account"
    },
    "trial_used_date": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of when the trial started, UTC"
    },
    "trial_card_fingerprint": {
      "type": "string",
      "description": "Stripe's payment method fingerprint captured from the checkout session, used to spot the same card being used across different accounts"
    },
    "flagged_for_review": {
      "type": "boolean",
      "default": false,
      "description": "Set to true when a new trial signup's card fingerprint matches a fingerprint already stored against a different User record, for manual admin review"
    }
  },
  "required": []
}